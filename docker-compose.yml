services:
  consul:
    image: hashicorp/consul:latest
    container_name: consul-server-v2
    ports:
      - "8500:8500"
      - "8600:8600/udp"
    command: "agent -dev -client=0.0.0.0"
    networks:
      - server-vpn

networks:
  server-vpn:
    driver: bridge